const colors = ["red", "green", "blue", "yellow", "orange"];

for(const color of colors) {
    console.log(color);
}

// Using for in looop 
//  loop is best used when you need to loop over
//  the properties of an object. This loop will iterate over all enumerable properties of an object, including inherited properties and non-numeric properties.
//Using for in loop is better when you need to loop over
